<h1>Atlassian's Random Lunch Location Generator</h1>

<table class="zebra-striped">
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @places.each do |place| %>
  <tr>
    <td><%= place.title %></td>
    <td><%= place.description %></td>
    <td><%= place.type %></td>
    <td><%= link_to 'Show', place %></td>
    <td><%= link_to 'Edit', edit_place_path(place) %></td>
    <td><%= link_to 'Destroy', place, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<strong> <%= link_to 'Add New Location', new_place_path %>     </strong>

<br> <br><br>
<b>Generate Random Lunch Location:</B>

<strong><%= link_to 'Click Me!', generate_lunch_places_path %> </strong>



